name: Doosan Bears Daily Newsletter

on:
  schedule:
    # KST(한국 시간) 아침 9시 = UTC(협정 세계시) 기준 00:00
    - cron: '0 0 * * *'
  workflow_dispatch: # 수동 실행 버튼 활성화

jobs:
  run-newsletter:
    runs-on: ubuntu-latest

    steps:
      # 1. 저장소 코드 가져오기
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. 파이썬 환경 세팅
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # 3. 필수 패키지 설치 (requirements.txt로 수정됨)
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # 4. 파이썬 스크립트 실행 및 디스코드 전송
      - name: Run final.py
        env:
          # 파이썬 스크립트 실행 단계 예시
      - name: Run final.py
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          NAVER_CLIENT_ID: ${{ secrets.NAVER_CLIENT_ID }}         # 👈 추가
          NAVER_CLIENT_SECRET: ${{ secrets.NAVER_CLIENT_SECRET }} # 👈 추가
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}           # 👈 추가 (Gemini 404 에러도 이걸로 같이 해결됩니다)
        run: python3 final.py
        run: python3 final.py
